/*
write a c++ program to read 10 intger number the find average of odd number using array.
*/
#include<iostream>
using namespace std;
int main(){
    int a[5];
    int sum = 0;
    for(int i = 0; i < 0 ; i++){
        cin>>a[i];
        if(a[i] % 2 i= 0)
        sum += a[i];
        avg = sum/i;
    }
    cout<<"The average of even number is : "<<avg;
    return 0;
}
